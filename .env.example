# Maestro Environment Variables

# ============ REQUIRED ============

# Anthropic API Key (required for agent execution)
# Get from: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# ============ AGENT WEBHOOK CONFIGURATION ============

# Agent webhook URLs (default: localhost, change for production)
FRONTEND_AGENT_WEBHOOK=http://localhost:3001/execute
BACKEND_AGENT_WEBHOOK=http://localhost:3002/execute
TESTING_AGENT_WEBHOOK=http://localhost:3003/execute

# ============ REDIS CONFIGURATION (for BullMQ) ============

# Redis connection (default: localhost)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Redis URL (alternative to individual settings)
# REDIS_URL=redis://localhost:6379

# ============ QUEUE CONFIGURATION ============

# Queue settings
QUEUE_CONCURRENCY=5
QUEUE_MAX_RETRIES=3
QUEUE_RETRY_DELAY=2000

# ============ SUPERVISOR CONFIGURATION ============

# Health check interval (milliseconds)
HEALTH_CHECK_INTERVAL=60000

# Bottleneck detection interval (milliseconds)
BOTTLENECK_CHECK_INTERVAL=300000

# ============ LOGGING ============

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# ============ ENVIRONMENT ============

# Node environment
NODE_ENV=development

# Uncomment for production
# NODE_ENV=production
# REDIS_URL=redis://your-redis-server:6379
